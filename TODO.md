## Issues

### 动机
- **为什么选择用 LLM 做中文拼写纠错，而不是继续用 BERT/SoftMaskedBERT/MacBERT 这类 encoder 模型？**
    >核心原因是：LLM 能把中文拼写纠错从“局部判别问题”提升为“全局条件生成问题”，在复杂语境下更有优势。
    具体有三点：
    第一，建模范式不同。
    BERT、SoftMaskedBERT、MacBERT 这类模型本质是做 token-level 分类或 mask 预测，需要先检测再替换，决策偏局部。
    LLM是直接建模 (P(y|x))，在生成过程中同时考虑整句语义一致性，不依赖显式的错误检测步骤，对多错误、联动错误更自然。
    第二，上下文与语义建模更强。
    很多拼写错误只有在具体语境下才成立，比如同音词、近义词误用。
    LLM（如 Qwen3.5）是在大规模语料上训练的自回归模型，对长距离依赖和语义一致性的建模能力更强，因此在复杂句子或长文本中纠错更稳定。
    第三，泛化能力更好。
    传统方法往往依赖混淆集，容易受限于预定义候选；LLM不依赖固定候选空间，而是通过语言建模概率选择最合理输出，对未见错误和开放场景更鲁棒。
    最后补一句我的工作：
    纯 LLM 对细粒度音近/形近不敏感，所以我在 LLM 中引入了拼音和字形特征的 Adapter，把传统 CSC 的先验信息和 LLM 的语义能力结合起来。


- **中文拼写纠错和语法纠错、文本润色/重写的边界是什么？你的方法主要解决哪一类错误？**
- **你说 LLM 容易过纠和幻觉，能具体举例说明吗？你的方案分别怎么缓解？**
    >结论：
    LLM 过纠的本质是“生成自由度过大 + 缺少显式错误约束”。我的方案从“输入约束 + 模型结构 + 训练目标 + 推理策略”四个层面做了抑制。
    第一，输入层面：只对源句建模错误信号。
    在 data collator 里，我只为 source tokens 构造拼音和字形特征，而 target 部分没有这些特征。这相当于显式告诉模型“哪里可能有错”，减少无关位置被修改的概率。
    第二，结构层面：局部注入而不是全局改写。
    我把 CSC Adapter 插在少数指定层（plug_idx），而不是改整个模型。
    同时用“门控残差”融合音形信息，本质是让模型在“保留原表示”和“使用纠错信息”之间自适应权衡，避免强制改写。
    第三，训练目标：限制只改必要位置。
    labels 只对 target 部分计算 loss，prompt 和 source 全部设为 -100。
    模型学到的是“在给定原句的条件下生成纠正结果”，而不是自由改写整句。
    第四，推理策略：弱化生成自由度。
    在 inference 时可以用低 temperature、greedy decoding，减少随机改写；同时输入是“原句 + 明确纠错指令”，进一步收缩生成空间。
    最后可以补一句 trade-off：
    如果约束过强，会导致漏纠。所以我用的是“软约束”（adapter + gating），而不是硬规则，这样在不过度改写和纠错能力之间取得平衡。


- **为什么汉字纠错需要字音、字形模态？语义上下文本身不够吗？**
- **这个任务的核心难点是 detection 还是 correction？你的方案主要提升了哪一个？**

### 方案设计

- **Adapter 为什么插在中间层，而不是 embedding 层、最后一层或者 LM head 前？**
- **你提到 “like Qwen3.5 28 layer”，这个层号是怎么选的？做过层位消融吗？**
- **为什么用字音字形作为 K/V，LLM hidden state 作为 Q？反过来可不可以？**
- **为什么用 cross-attention，而不是简单 concat、add、FiLM、LoRA 或 prefix tuning？**
- **门控机制具体解决什么问题？gate 的输入为什么是 `[residual, attn_output]`？**
- **Adapter 输出是 residual add，你如何避免破坏原模型原有语义能力？**
- **字音、字形信息是对每个 token 都注入吗？非中文 token、BPE token、标点怎么处理？**
- **如果 tokenizer 不是字符级，而是 BPE，字音字形如何和 token 对齐？**
- **“字符级分词 + 后训练”具体怎么做？是重训 tokenizer，还是 tokenizer pruning？**
- **字符级分词会导致序列变长，训练和推理成本上升，你怎么权衡？**
    >确实，字符级分词会让序列变长，带来训练和推理开销。但在 CSC 任务里，我认为这个成本是可以接受的，因为它换来的是更重要的纠错粒度一致性。
    CSC 的错误通常发生在单个汉字层面，比如音近字、形近字替换。如果使用普通 BPE/词级 token，一个词可能被整体编码，模型很难精确定位到词内部哪个字错了，也不利于和拼音、字形特征对齐。字符级分词可以让 token 粒度和错误粒度一致，便于做检测、纠正和音形特征注入。
    成本方面，我没有把所有 token 都字符化，而是主要裁剪多字中文 token，让中文部分更接近字符粒度；英文、数字、标点等仍然保留原 tokenizer 的能力。另外 CSC 输入通常是句子级或短文本级任务，序列长度增长相对可控，不像长文生成那样成本爆炸。
    所以我的权衡是：为了提升错误定位、音形对齐和纠错可控性，接受一定的序列长度开销；同时通过只裁剪中文多字 token、控制 max length、只在少数层插入 adapter 等方式控制训练和推理成本。


### 训练细节
- **你冻结模型主干，只训练 adapter 和模态编码器。为什么还要不要训练目标层 MLP？**
- **如果只训 adapter，效果会下降多少？如果放开更多层，会不会更好？**
- **训练数据里 10% 通用数据的作用是什么？怎么确定比例？**
- **正样本和负样本比例是多少？如何避免模型学成“见句就改”？**
- **训练 objective 是普通 causal LM loss 吗？有没有对错误位置加权？**
    >结论：
    训练目标是标准的 causal LM loss（token-level cross entropy），没有显式对“错误位置”做额外加权。
    具体设计是：
    我把输入组织成「prompt + source + target」，然后只在 target 部分计算 loss，prompt 和 source 全部设为 -100。这样模型学的是“在给定原句的条件下生成纠正句”，而不是复现输入。
    关于错误位置加权，我当前没有做显式的 loss reweight。主要有两个考虑：
    第一，LLM 的自回归训练本身会对“需要被修改的位置”产生更大的梯度信号。因为这些位置如果不改，loss 会明显更高，相当于一种隐式加权。
    第二，我把“错误感知”更多交给结构而不是 loss，比如引入拼音和字形特征的 CSC Adapter，并且只在 source token 上注入这些特征，让模型在表示层就能区分“可能出错的位置”。
    最后可以补一句扩展思考：
    如果进一步优化，可以考虑做 position-aware weighting，比如对 source 和 target 的 diff 位置提高权重，或者引入 detection 辅助任务做 multi-task learning，但当前方案优先保持训练稳定性和实现简洁。

- **拼写错误是天然稀疏的，loss 大部分来自不变 token，你怎么处理类别不平衡？**
    >确实，CSC 里错误 token 很稀疏，如果直接对整句 target 做 causal LM loss，不变 token 会占主要部分，容易让模型学成“尽量复制原句”。
    我的处理主要有三点。
    第一，训练数据层面提高错误样本比例。
    数据构造时使用混淆集或生成式方式构造含错样本，保证 batch 中有足够的纠错信号，而不是大量纯净句子稀释梯度。
    第二，结构层面强化错误相关特征。
    我引入拼音和字形 Adapter，让模型在 source 端显式看到音近、形近线索。这样即使 loss 没有显式加权，模型也更容易在表示层捕捉到可能错误的位置。
    第三，目标函数上当前保持标准 causal LM loss，没有额外错误位置加权。
    这是为了保证训练稳定，避免模型过度关注 diff 位置导致过纠。因为 CSC 不只是改错，还要求大量位置保持不变，保留能力本身也是任务目标。
    可以补一句改进方向：
    如果后续继续优化，我会考虑基于 source-target diff 对错误位置做轻量加权，或者增加 detection 辅助 loss，但权重不能太大，否则会牺牲 precision，增加过纠。

- **训练时 OOM 你会怎么排查和优化？**
- **分布式训练具体用了 DDP、DeepSpeed 还是 FSDP？DDP 能解决单卡 OOM 吗？**
    >我这里主要用的是 **DDP + DeepSpeed ZeRO-3**，不是 FSDP。
    DDP 本身不能解决单卡 OOM。DDP 的核心作用是数据并行，每张卡都保留一份完整模型，只是把 batch 切到多张卡上并同步梯度。因此它能提升吞吐、扩大 effective batch size，但不能显著降低单卡模型显存占用。
    真正缓解单卡 OOM 的是 **DeepSpeed ZeRO**。
    ZeRO-1 切 optimizer states，ZeRO-2 进一步切 gradients，ZeRO-3 会把模型参数、梯度和优化器状态都做分片，所以单卡不用保存完整训练状态，显存压力会明显下降。
    在我的项目里，因为底座是 Qwen3.5-9B 级别模型，单纯 DDP 不够，所以训练脚本里用的是 DeepSpeed ZeRO-3。同时我冻结了大部分 LLM 参数，只训练 CSC Adapter 和部分 MLP，再配合 gradient checkpointing、fp16/bf16 来控制显存。

- **字形 cache 和拼音 cache 是怎么构建的？训练时会不会成为 dataloader 瓶颈？**

### 实验与指标
- **你说 correction F1 71%、detection F1 79%，这是在哪个数据集、哪个评价脚本上的结果？**
- **SFT 62%、ICL 48% 是同一基座、同一数据、同一指标下比较的吗？**
- **过纠比例降低 20%/17%，过纠比例怎么定义？**
- **你是否区分 sentence-level F1 和 char-level F1？哪个更重要？**
- **SIGHAN、LEMON、ECSpell、CSCD-NS 这些数据分布有什么差异？**
    >结论：
    这些数据集的主要差异体现在“错误来源（人工 vs 合成）”“文本域（学习者文本 vs 通用语料）”“难度（是否包含语义级错误）”三个维度上，因此它们分别考察模型的不同能力，不能简单混用结论。
    具体来看：
    **SIGHAN**
    这是最经典的 CSC 基准，主要是学习者语料中的真实拼写错误，特点是数据规模较小、错误类型以音近/形近为主，句子相对规范。
    它更偏“标准评测集”，但分布简单，容易被过拟合或高估模型能力。
    **LEMON**
    这是较新的数据集，强调更真实、更复杂的错误分布。相比 SIGHAN，它的错误类型更丰富，不仅有音形错误，还包含一定语义级误用，文本更接近自然语料。
    对模型的泛化能力和语境理解要求更高。
    **ECSpell**
    这是一个大规模构造数据集，主要通过混淆集自动生成错误。优点是规模大、可用于训练；缺点是分布偏“规则化”，错误类型相对单一，和真实数据存在 gap。
    模型在 ECSpell 上表现好，不一定代表在真实场景表现好。
    **CSCD-NS**
    这是更偏“自然场景”的数据集（NS = Natural Scenario），错误来源更接近真实输入（如用户输入、噪声文本）。
    通常包含更复杂、更隐蔽的错误，以及更强的语义干扰，对鲁棒性要求更高。
    最后可以总结一句：
    SIGHAN 更像标准 benchmark，ECSpell 适合做训练数据，LEMON 和 CSCD-NS 更能反映真实场景下的泛化能力。我在评测时会同时报告多数据集结果，避免单一数据分布带来的偏差。

- **你的方法对音近错误、形近错误、语法错误、语义错误分别表现如何？**
- **有没有做 clean sentence 测试？也就是输入完全正确时，模型保持不改的能力。**
- **有做消融吗：去掉拼音、去掉字形、去掉 gate、去掉 cross-attention、不同插层位置？**
- **模型规模换成 Qwen3-8B、Qwen3.5-9B 或更小模型时，收益是否一致？**
- **你的提升来自 adapter 结构，还是来自字符级 tokenizer/后训练，怎么证明？**

### 工程与落地
- **推理时每个 token 都要构造字音字形特征，延迟增加多少？**
- **如果部署在线纠错服务，怎么做 batch、cache 和吞吐优化？**
- **字形渲染依赖字体，如果换字体或遇到生僻字，稳定性如何？**
- **拼音多音字怎么处理？只用 pypinyin 默认读音会不会引入噪声？**
- **如果用户输入包含英文、数字、emoji、繁体字，系统怎么处理？**
- **这个方案在长文本纠错中会遇到什么问题？**
    >结论：
    这个方案在长文本场景下主要会遇到三类问题：**上下文建模退化、计算开销放大、以及过纠风险累积**。
    第一，上下文建模问题。
    虽然 LLM 理论上能处理长上下文，但在实际中，随着序列变长，注意力会被稀释，模型更偏向局部决策。CSC 又是一个对局部精确性要求很高的任务，这会导致远距离语义约束变弱，出现“局部合理但全局不一致”的纠错。
    第二，计算与资源问题。
    字符级分词本身会放大序列长度，而 Transformer 的 attention 是 (O(n^2))。在长文本下，显存和推理延迟都会显著上升。再叠加拼音和字形特征构造（尤其是 glyph 渲染），整体开销会进一步增加。
    第三，过纠的累积效应。
    长文本中 token 数量多，即使单点过纠概率不高，累积起来也容易出现明显的改写偏移，比如风格漂移、非错误位置被修改。这在生成式 LLM 中更明显。
    第四，错误密度不均的问题。
    长文本通常只有局部片段存在错误，大量 token 是正确的。如果用统一的生成式目标，容易被“复制原文”主导，导致召回下降（漏纠）。
    可以补一句你的方案局限：
    当前是单段输入 + 单次生成，没有显式的“分段校验”或“检测-纠错分离机制”，在长文本下鲁棒性会下降。
    最后给出改进方向（面试加分点）：
    可以考虑 sliding window / 分句处理、先做 detection 再做 correction、或者引入编辑约束（edit-based decoding），来在长文本场景下控制计算成本和过纠问题。


### 追问型问题
- **如果线上发现模型过纠严重，你优先改数据、loss、prompt 还是模型结构？**
- **如果 detection 高但 correction 低，说明什么？你会怎么改？**
- **如果 correction precision 高但 recall 低，你会怎么调？**
- **如果让你把这个方案压到 7B 以下甚至 1.5B 模型，你会怎么做？**
- **这个方案相比纯 SFT 最大的不可替代价值是什么？**
    >结论：
    相比纯 SFT，这个方案的**不可替代价值在于显式引入“音近 + 形近”的归纳偏置**，把 CSC 从“纯语义生成”转成“语义 + 语言学先验联合建模”，在低频错误和分布外场景下更稳。
    具体有三点：
    第一，补齐 LLM 的感知盲区。
    纯 SFT 的 LLM 主要依赖语义和上下文概率，对“发音相同但语义不通”“字形相似但语义错误”的情况不敏感。
    我通过拼音和字形编码，把这些信息显式注入模型，使模型在表示层就能区分“语义合理但拼写错误”的情况。
    第二，提高对低频和未见错误的泛化能力。
    纯 SFT 很依赖训练数据分布，本质是记忆式泛化。
    而音形特征是一种结构化先验，比如同音字、形近字之间的关系是可泛化的，因此在 OOD 错误（没见过的错别字）上更有优势。
    第三，更好的 precision–recall 平衡。
    纯 SFT 往往要么保守（漏纠），要么激进（过纠）。
    我的方案通过 adapter + gating 机制，把纠错信息作为“可控增量”注入，让模型在“是否修改”上有更细粒度的决策空间，从而更容易控制过纠。
    最后可以补一句总结：
    纯 SFT 是“让模型自己学纠错”，而我的方案是“在模型里显式建模纠错所需的语言学结构”，这部分归纳偏置是很难通过单纯数据规模完全替代的。

- **你觉得这个方法最大的缺陷是什么？下一步怎么改？**
    >我认为最大的缺陷是：**当前方法仍然是生成式纠错，缺少显式的“编辑约束”和“错误检测监督”**。
    虽然我用拼音、字形 Adapter 给 LLM 注入了 CSC 先验，但最终输出仍然依赖 causal LM generation。这会带来两个问题：第一，模型可能修改本来正确的位置，产生过纠；第二，错误很稀疏时，普通 LM loss 容易被大量不变 token 主导，导致漏纠或复制倾向。
    下一步我会从两方面改。
    第一，引入 detection 辅助任务。
    在 Adapter 或中间层增加一个 token-level detection head，预测每个位置是否需要修改。这样模型不仅学习“怎么改”，也学习“哪里该改”。推理时可以用 detection 结果约束生成或后处理，降低过纠。
    第二，引入 edit-based correction。
    相比直接生成完整句子，可以让模型输出编辑操作，比如 keep / replace / delete / insert，或者只生成需要替换的位置。这样更符合 CSC 的任务形态，也能降低长文本下的生成成本和过纠风险。
    最后可以补一句：
    所以当前方案的优势是把音形先验注入 LLM，但缺陷是输出层仍然不够可控。后续我会把它从“自由生成式纠错”进一步改成“检测约束下的编辑式纠错”。


### 更强压迫感的问题
- **你的 adapter 其实也是额外参数微调，为什么不直接 LoRA 全层？**
    >结论：
    我没有直接用全层 LoRA，是因为 CSC 是一个**强结构先验任务（音近/形近）**，而 LoRA 本质只是参数高效微调，缺少这种显式归纳偏置；我的 adapter 更像是在模型里“加能力”，而不是只“调参数”。
    具体有三点差异：
    第一，归纳偏置不同。
    LoRA 是对注意力或 FFN 做低秩更新，本质仍是让模型从数据中隐式学习纠错规律；但 CSC 很依赖音近、形近这种结构信息，仅靠 LoRA 很难稳定学到。
    我的 adapter 是显式引入拼音和字形特征，这部分信息是 LoRA 无法直接表达的。
    第二，修改范围与可控性。
    全层 LoRA 会影响所有层的表示，容易引入分布漂移，在 CSC 这种“以保持原句为主”的任务上，容易加剧过纠。
    我只在少数层插入 adapter，并用 gating 控制信息注入强度，本质是“局部、可控地修改表示”。
    第三，参数利用效率。
    LoRA 虽然参数高效，但如果做全层，实际参与训练的参数仍然不小，而且每一层都在学习类似能力，存在冗余。
    adapter 是针对 CSC 任务设计的模块，参数更集中在“音形建模 + 融合”，信息密度更高。
    最后可以补一句 trade-off（加分点）：
    LoRA 的优点是通用、实现简单，如果只是做 baseline 或快速适配是更好的选择；而我的方案更偏 task-specific，在 CSC 上有更强的表达能力，但工程复杂度更高。

- **你说“保留语义理解能力”，有没有用通用 benchmark 验证灾难性遗忘？**
- **你的方法是否只是对拼音/字形错误有效？对真实用户输入是否泛化？**
- **如果面试官要求你现场画出 forward 数据流，你能从 `input_ids` 一直讲到 `loss` 吗？**

**key points**：`插入位置`、`门控机制`、`K/V 设计`、`10% 通用数据比例`、`过纠定义`、`消融实验`、`OOM 解决`、`评价指标口径`


## Solutions

1. qwen3.5-9b full sft
2. qwen3.5-9b-char full sft
1.1 add adapter multimodal
2.1 add adapter multimodal